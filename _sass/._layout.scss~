// Ads
.top-ad {
  background-color: #76a6a6;
}

.leaderboard {
  margin-top: 22px;
  
  @media screen and (max-width: 960px) {
    margin-top: 0px; 
  }

}

.logo {
  float: left;
  margin: 0;
}

// Wrap
.wrap {
  max-width: 1920px;
  min-height: 100%;
  display: block;
  margin-bottom: -$footer-height;
  margin-left: auto;
  margin-right: auto;
  background-image: -ms-linear-gradient(bottom, #96D296 0%, #EFEFEF 100%); /* IE10 Consumer Preview */ 
  background-image: -moz-linear-gradient(bottom, #96D296 0%, #EFEFEF 100%); /* Mozilla Firefox */ 
  background-image: -o-linear-gradient(bottom, #96D296 0%, #EFEFEF 100%); /* Opera */ 
  background-image: -webkit-gradient(linear, left bottom, left top, color-stop(0, #96D296), color-stop(1, #EFEFEF)); /* Webkit (Safari/Chrome 10) */ 
  background-image: -webkit-linear-gradient(bottom, #96D296 0%, #EFEFEF 100%); /* Webkit (Chrome 11+) */
}

// Styling for mobile devices
@media screen and (max-width: 768px) {
  .wrap {
    margin-bottom: -$footer-height * 3.9;
    background-image: -moz-linear-gradient(bottom, #96D296 0%, #EFEFEF 100%); /* Mozilla Firefox */ 
    background-image: -o-linear-gradient(bottom, #96D296 0%, #EFEFEF 100%); /* Opera */ 
    background-image: -webkit-gradient(linear, left bottom, left top, color-stop(0, #96D296), color-stop(1, #EFEFEF)); /* Webkit (Safari/Chrome 10) */
  }
}

.wrap:after {
  content: "";
  display: block;
}


// Pages
.page-content {
    padding: $spacing-unit;
}

.page-heading {
    font-size: 20px;
}

// Posts

.post-header {
    margin: $spacing-unit;
}

.post-title {
    font-size: 2em;
    letter-spacing: -1px;
    line-height: 1;
}

.post-content {
  margin-bottom: $spacing-unit;
  padding: 0 $spacing-unit $spacing-unit;
}

.post-list {
    margin-left: 0;
    list-style: none;

    > li {
        margin-bottom: $spacing-unit;
    }
}

.post-meta {
    font-size: $small-font-size;
    color: darken($complimentary-color, 35%)
}

.post-link {
    display: block;
    font-size: 24px;
}

article p{
  background-color: #EFEFEF;
  margin: 10px 0px 0px 0px;
  padding-left: 10px;
}

// Footer
#footer {
  background-color: $footer-color;
}

.site-footer, .wrap:after {
  height: $footer-height;
}
.site-footer {
  background: $footer-color;
}

// Styling for mobile devices
@media screen and (max-width: 768px) {
  .site-footer, .wrap:after {
    height: $footer-height * 3.9; 
  }
  
  .site-footer ul {
    margin: 0; 
  }
  
  .site-footer li {
    list-style-type: none;
    height: 40px;
    line-height: 40px;
  }
  
  .site-footer span.socials {
    color: $secondary-color;
  }
  
  .site-footer span.icon-gumroad {
    font-size: 2.5em;
  }
}

// Asides
.updates {
  text-align: center;
  color: white;
  margin-bottom: 0px;
}

// Copyright
.copyright {
  font-size: $small-font-size;
  margin: 10px 0 0 0;
}
